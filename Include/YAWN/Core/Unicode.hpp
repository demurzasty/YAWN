#pragma once 

#include "Array.hpp"

namespace YAWN {
    class Unicode {
    public:
        //static int Length(const char* utf8);

        //static int ToUTF32(char32_t& utf32, const char* utf8);

        //static int ToUTF8(char* utf8, const char32_t utf32);

        //static void ToUTF32(char32_t* utf32, const char* utf8, int length);

        //static int ToUTF8(char* utf8, const char32_t* utf32, int length);

        //static void ToUTF8(char* utf8, const wchar_t* wide, int length);

        //static int Compare(const char32_t* str1, const char32_t* str2, int length);

        //static int Compare(const wchar_t* str1, const wchar_t* str2, int length);
    };
}
